{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"gitdevflow","text":"<p>Welcome to gitdevflow \u2014 a developer-friendly CLI tool for streamlining Git-based development workflows.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>PR Management \u2014 Create, list, and manage pull requests from the terminal</li> <li>Changelog Generation \u2014 Automatically generate changelogs from merged PRs</li> <li>Configuration \u2014 Flexible YAML-based configuration with environment variable overrides</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>pip install gitdevflow\ngitdevflow --help\n</code></pre>"},{"location":"#navigation","title":"Navigation","text":"<ul> <li>Installation</li> <li>PR Commands</li> <li>Changelog</li> <li>Configuration</li> </ul>"},{"location":"contributing/","title":"Contributing to gitdevflow","text":"<p>Thank you for your interest in contributing to gitdevflow!</p> <p>Please review our Contributing Guidelines and Code of Conduct.</p>"},{"location":"contributing/#local-development-workflow","title":"Local Development Workflow","text":"<ol> <li> <p>Clone Repository &amp; Install Dependencies:    <pre><code>git clone https://github.com/BlockForge-Dev/gitdevflow.git\ncd gitdevflow\npoetry install --with dev,docs\n</code></pre></p> </li> <li> <p>Pre-commit Hooks:    <pre><code>poetry run pre-commit install\n</code></pre></p> </li> <li> <p>Running Quality Checks:    <pre><code>poetry run ruff check --fix src/ tests/\npoetry run black src/ tests/\npoetry run mypy src/\npoetry run pytest\n</code></pre></p> </li> <li> <p>Building Documentation:    <pre><code>poetry run mkdocs serve\n</code></pre></p> </li> </ol>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+</li> <li>Git 2.30+</li> <li>A GitHub personal access token (for API operations)</li> </ul>"},{"location":"installation/#install-from-pypi","title":"Install from PyPI","text":"<pre><code>pip install gitdevflow\n</code></pre> <p>Direct PyPI Package: https://pypi.org/project/gitdevflow/</p>"},{"location":"installation/#install-from-source","title":"Install from source","text":"<pre><code>git clone https://github.com/BlockForge-Dev/gitdevflow.git\ncd gitdevflow\npip install -e \".[dev]\"\n</code></pre>"},{"location":"installation/#configuration","title":"Configuration","text":"<p>After installation, create a configuration file:</p> <pre><code>gitdevflow config init\n</code></pre> <p>Set your GitHub token as an environment variable:</p> <pre><code>export GITHUB_TOKEN=\"ghp_your_token_here\"\n</code></pre> <p>Or add it to your <code>.env</code> file. See Configuration for more details.</p>"},{"location":"reference/api/","title":"API Reference","text":"<p>This page provides auto-generated API documentation for <code>gitdevflow</code> core modules and clients.</p>"},{"location":"reference/api/#github-client","title":"GitHub Client","text":""},{"location":"reference/api/#gitdevflow.core.github_client","title":"<code>gitdevflow.core.github_client</code>","text":"<p>Async GitHub REST API client with resilience, retries, and pagination.</p>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient","title":"<code>GitHubClient</code>","text":"<p>Async wrapper around the GitHub REST API with retries and pagination.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>class GitHubClient:\n    \"\"\"Async wrapper around the GitHub REST API with retries and pagination.\"\"\"\n\n    BASE_URL = \"https://api.github.com\"\n\n    def __init__(\n        self,\n        token: str,\n        owner: str = \"\",\n        repo: str = \"\",\n        timeout: float = 30.0,\n        transport: httpx.AsyncBaseTransport | None = None,\n    ) -&gt; None:\n        self.owner = owner\n        self.repo = repo\n        headers = {\n            \"Accept\": \"application/vnd.github+json\",\n            \"X-GitHub-Api-Version\": \"2022-11-28\",\n        }\n        if token:\n            headers[\"Authorization\"] = f\"Bearer {token}\"\n\n        self._client = httpx.AsyncClient(\n            base_url=self.BASE_URL,\n            headers=headers,\n            timeout=timeout,\n            transport=transport,\n        )\n\n    def _handle_error_response(self, response: httpx.Response) -&gt; None:\n        \"\"\"Map HTTP error status codes to custom gitdevflow exceptions.\"\"\"\n        if response.is_success:\n            return\n\n        status = response.status_code\n        try:\n            payload = response.json()\n            message = payload.get(\"message\", response.text)\n        except Exception:\n            message = response.text or f\"HTTP status {status}\"\n\n        if status == 401:\n            raise AuthenticationError(message)\n        if status in (429, 403) and (\"rate limit\" in message.lower() or status == 429):\n            retry_after = response.headers.get(\"Retry-After\")\n            seconds = (\n                int(retry_after) if retry_after and retry_after.isdigit() else None\n            )\n            raise RateLimitedError(message, retry_after=seconds)\n        if status == 404:\n            raise NotFoundError(message)\n\n        raise GitHubAPIError(status, message)\n\n    @retry(\n        retry=retry_if_exception_type((_TransientHTTPError, httpx.TransportError)),\n        stop=stop_after_attempt(3),\n        wait=wait_exponential(multiplier=0.5, min=0.5, max=4.0),\n        reraise=True,\n    )\n    async def _send_request(\n        self,\n        method: str,\n        url: str,\n        params: dict[str, Any] | None = None,\n        json: dict[str, Any] | None = None,\n    ) -&gt; httpx.Response:\n        \"\"\"Send HTTP request with automatic retries for transient errors.\"\"\"\n        response = await self._client.request(method, url, params=params, json=json)\n        if response.status_code in (429, 500, 502, 503, 504):\n            raise _TransientHTTPError(f\"Transient HTTP error: {response.status_code}\")\n        return response\n\n    async def _request(\n        self,\n        method: str,\n        url: str,\n        params: dict[str, Any] | None = None,\n        json: dict[str, Any] | None = None,\n    ) -&gt; httpx.Response:\n        \"\"\"Execute request with retries and error mapping.\"\"\"\n        try:\n            response = await self._send_request(method, url, params=params, json=json)\n        except _TransientHTTPError as err:\n            last_code = int(str(err).split(\":\")[-1].strip())\n            if last_code == 429:\n                raise RateLimitedError(\"GitHub API rate limit exceeded\") from err\n            raise GitHubAPIError(\n                last_code, f\"Server error after retries: {last_code}\"\n            ) from err\n\n        self._handle_error_response(response)\n        return response\n\n    async def paginate(\n        self,\n        url: str,\n        params: dict[str, Any] | None = None,\n        per_page: int = 30,\n    ) -&gt; AsyncGenerator[dict[str, Any], None]:\n        \"\"\"Yield items across paginated GitHub endpoints using RFC 5988 Link headers.\"\"\"\n        next_url: str | None = url\n        query_params = dict(params or {})\n        query_params[\"per_page\"] = per_page\n\n        while next_url:\n            response = await self._request(\"GET\", next_url, params=query_params)\n            data = response.json()\n            if isinstance(data, list):\n                for item in data:\n                    yield cast(dict[str, Any], item)\n            else:\n                yield cast(dict[str, Any], data)\n\n            link_header = response.headers.get(\"Link\")\n            next_url = None\n            query_params = {}\n\n            if link_header:\n                matches = re.findall(r'&lt;([^&gt;]+)&gt;;\\s*rel=\"([^\"]+)\"', link_header)\n                for link, rel in matches:\n                    if rel == \"next\":\n                        next_url = link\n                        break\n\n    async def get_repo(self, owner: str, repo: str) -&gt; Repository:\n        \"\"\"Get repository details.\"\"\"\n        response = await self._request(\"GET\", f\"/repos/{owner}/{repo}\")\n        return Repository.model_validate(response.json())\n\n    async def get_pull_requests(\n        self,\n        owner: str,\n        repo: str,\n        state: str = \"open\",\n        per_page: int = 30,\n    ) -&gt; list[PullRequest]:\n        \"\"\"Get pull requests for a repository with pagination.\"\"\"\n        prs: list[PullRequest] = []\n        async for item in self.paginate(\n            f\"/repos/{owner}/{repo}/pulls\",\n            params={\"state\": state},\n            per_page=per_page,\n        ):\n            prs.append(PullRequest.model_validate(item))\n        return prs\n\n    async def create_pull_request(\n        self,\n        owner: str,\n        repo: str,\n        title: str,\n        head: str,\n        base: str = \"main\",\n        body: str | None = None,\n        draft: bool = False,\n    ) -&gt; PullRequest:\n        \"\"\"Create a new pull request.\"\"\"\n        payload: dict[str, Any] = {\n            \"title\": title,\n            \"head\": head,\n            \"base\": base,\n            \"draft\": draft,\n        }\n        if body:\n            payload[\"body\"] = body\n\n        response = await self._request(\n            \"POST\", f\"/repos/{owner}/{repo}/pulls\", json=payload\n        )\n        return PullRequest.model_validate(response.json())\n\n    async def get_labels(self, owner: str, repo: str) -&gt; list[Label]:\n        \"\"\"Get repository labels.\"\"\"\n        labels: list[Label] = []\n        async for item in self.paginate(f\"/repos/{owner}/{repo}/labels\"):\n            labels.append(Label.model_validate(item))\n        return labels\n\n    async def set_labels(\n        self,\n        owner: str,\n        repo: str,\n        issue_number: int,\n        labels: list[str],\n    ) -&gt; list[Label]:\n        \"\"\"Set labels on a PR or issue.\"\"\"\n        response = await self._request(\n            \"POST\",\n            f\"/repos/{owner}/{repo}/issues/{issue_number}/labels\",\n            json={\"labels\": labels},\n        )\n        data = response.json()\n        return [Label.model_validate(item) for item in data]\n\n    async def merge_pull_request(\n        self,\n        owner: str,\n        repo: str,\n        pr_number: int,\n        merge_method: str = \"squash\",\n    ) -&gt; dict[str, Any]:\n        \"\"\"Merge a pull request.\"\"\"\n        response = await self._request(\n            \"PUT\",\n            f\"/repos/{owner}/{repo}/pulls/{pr_number}/merge\",\n            json={\"merge_method\": merge_method},\n        )\n        return cast(dict[str, Any], response.json())\n\n    async def compare_branches(\n        self,\n        owner: str,\n        repo: str,\n        base: str,\n        head: str,\n    ) -&gt; BranchComparison:\n        \"\"\"Compare two branches or commits.\"\"\"\n        response = await self._request(\n            \"GET\", f\"/repos/{owner}/{repo}/compare/{base}...{head}\"\n        )\n        return BranchComparison.model_validate(response.json())\n\n    async def get_commit_pull_requests(\n        self, owner: str, repo: str, commit_sha: str\n    ) -&gt; list[PullRequest]:\n        \"\"\"Fetch pull requests associated with a commit SHA.\"\"\"\n        prs: list[PullRequest] = []\n        async for item in self.paginate(\n            f\"/repos/{owner}/{repo}/commits/{commit_sha}/pulls\"\n        ):\n            prs.append(PullRequest.model_validate(item))\n        return prs\n\n    async def close(self) -&gt; None:\n        \"\"\"Close the underlying HTTP client.\"\"\"\n        await self._client.aclose()\n\n    async def __aenter__(self) -&gt; GitHubClient:\n        return self\n\n    async def __aexit__(self, *args: Any) -&gt; None:\n        await self.close()\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.close","title":"<code>close()</code>  <code>async</code>","text":"<p>Close the underlying HTTP client.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def close(self) -&gt; None:\n    \"\"\"Close the underlying HTTP client.\"\"\"\n    await self._client.aclose()\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.compare_branches","title":"<code>compare_branches(owner, repo, base, head)</code>  <code>async</code>","text":"<p>Compare two branches or commits.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def compare_branches(\n    self,\n    owner: str,\n    repo: str,\n    base: str,\n    head: str,\n) -&gt; BranchComparison:\n    \"\"\"Compare two branches or commits.\"\"\"\n    response = await self._request(\n        \"GET\", f\"/repos/{owner}/{repo}/compare/{base}...{head}\"\n    )\n    return BranchComparison.model_validate(response.json())\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.create_pull_request","title":"<code>create_pull_request(owner, repo, title, head, base='main', body=None, draft=False)</code>  <code>async</code>","text":"<p>Create a new pull request.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def create_pull_request(\n    self,\n    owner: str,\n    repo: str,\n    title: str,\n    head: str,\n    base: str = \"main\",\n    body: str | None = None,\n    draft: bool = False,\n) -&gt; PullRequest:\n    \"\"\"Create a new pull request.\"\"\"\n    payload: dict[str, Any] = {\n        \"title\": title,\n        \"head\": head,\n        \"base\": base,\n        \"draft\": draft,\n    }\n    if body:\n        payload[\"body\"] = body\n\n    response = await self._request(\n        \"POST\", f\"/repos/{owner}/{repo}/pulls\", json=payload\n    )\n    return PullRequest.model_validate(response.json())\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.get_commit_pull_requests","title":"<code>get_commit_pull_requests(owner, repo, commit_sha)</code>  <code>async</code>","text":"<p>Fetch pull requests associated with a commit SHA.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def get_commit_pull_requests(\n    self, owner: str, repo: str, commit_sha: str\n) -&gt; list[PullRequest]:\n    \"\"\"Fetch pull requests associated with a commit SHA.\"\"\"\n    prs: list[PullRequest] = []\n    async for item in self.paginate(\n        f\"/repos/{owner}/{repo}/commits/{commit_sha}/pulls\"\n    ):\n        prs.append(PullRequest.model_validate(item))\n    return prs\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.get_labels","title":"<code>get_labels(owner, repo)</code>  <code>async</code>","text":"<p>Get repository labels.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def get_labels(self, owner: str, repo: str) -&gt; list[Label]:\n    \"\"\"Get repository labels.\"\"\"\n    labels: list[Label] = []\n    async for item in self.paginate(f\"/repos/{owner}/{repo}/labels\"):\n        labels.append(Label.model_validate(item))\n    return labels\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.get_pull_requests","title":"<code>get_pull_requests(owner, repo, state='open', per_page=30)</code>  <code>async</code>","text":"<p>Get pull requests for a repository with pagination.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def get_pull_requests(\n    self,\n    owner: str,\n    repo: str,\n    state: str = \"open\",\n    per_page: int = 30,\n) -&gt; list[PullRequest]:\n    \"\"\"Get pull requests for a repository with pagination.\"\"\"\n    prs: list[PullRequest] = []\n    async for item in self.paginate(\n        f\"/repos/{owner}/{repo}/pulls\",\n        params={\"state\": state},\n        per_page=per_page,\n    ):\n        prs.append(PullRequest.model_validate(item))\n    return prs\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.get_repo","title":"<code>get_repo(owner, repo)</code>  <code>async</code>","text":"<p>Get repository details.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def get_repo(self, owner: str, repo: str) -&gt; Repository:\n    \"\"\"Get repository details.\"\"\"\n    response = await self._request(\"GET\", f\"/repos/{owner}/{repo}\")\n    return Repository.model_validate(response.json())\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.merge_pull_request","title":"<code>merge_pull_request(owner, repo, pr_number, merge_method='squash')</code>  <code>async</code>","text":"<p>Merge a pull request.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def merge_pull_request(\n    self,\n    owner: str,\n    repo: str,\n    pr_number: int,\n    merge_method: str = \"squash\",\n) -&gt; dict[str, Any]:\n    \"\"\"Merge a pull request.\"\"\"\n    response = await self._request(\n        \"PUT\",\n        f\"/repos/{owner}/{repo}/pulls/{pr_number}/merge\",\n        json={\"merge_method\": merge_method},\n    )\n    return cast(dict[str, Any], response.json())\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.paginate","title":"<code>paginate(url, params=None, per_page=30)</code>  <code>async</code>","text":"<p>Yield items across paginated GitHub endpoints using RFC 5988 Link headers.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def paginate(\n    self,\n    url: str,\n    params: dict[str, Any] | None = None,\n    per_page: int = 30,\n) -&gt; AsyncGenerator[dict[str, Any], None]:\n    \"\"\"Yield items across paginated GitHub endpoints using RFC 5988 Link headers.\"\"\"\n    next_url: str | None = url\n    query_params = dict(params or {})\n    query_params[\"per_page\"] = per_page\n\n    while next_url:\n        response = await self._request(\"GET\", next_url, params=query_params)\n        data = response.json()\n        if isinstance(data, list):\n            for item in data:\n                yield cast(dict[str, Any], item)\n        else:\n            yield cast(dict[str, Any], data)\n\n        link_header = response.headers.get(\"Link\")\n        next_url = None\n        query_params = {}\n\n        if link_header:\n            matches = re.findall(r'&lt;([^&gt;]+)&gt;;\\s*rel=\"([^\"]+)\"', link_header)\n            for link, rel in matches:\n                if rel == \"next\":\n                    next_url = link\n                    break\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.github_client.GitHubClient.set_labels","title":"<code>set_labels(owner, repo, issue_number, labels)</code>  <code>async</code>","text":"<p>Set labels on a PR or issue.</p> Source code in <code>src/gitdevflow/core/github_client.py</code> <pre><code>async def set_labels(\n    self,\n    owner: str,\n    repo: str,\n    issue_number: int,\n    labels: list[str],\n) -&gt; list[Label]:\n    \"\"\"Set labels on a PR or issue.\"\"\"\n    response = await self._request(\n        \"POST\",\n        f\"/repos/{owner}/{repo}/issues/{issue_number}/labels\",\n        json={\"labels\": labels},\n    )\n    data = response.json()\n    return [Label.model_validate(item) for item in data]\n</code></pre>"},{"location":"reference/api/#configuration","title":"Configuration","text":""},{"location":"reference/api/#gitdevflow.core.config","title":"<code>gitdevflow.core.config</code>","text":"<p>Pydantic settings loaded from YAML configuration and environment variables.</p>"},{"location":"reference/api/#gitdevflow.core.config.AppConfig","title":"<code>AppConfig</code>","text":"<p>               Bases: <code>BaseSettings</code></p> <p>Application configuration with YAML and environment variable support.</p> Source code in <code>src/gitdevflow/core/config.py</code> <pre><code>class AppConfig(BaseSettings):\n    \"\"\"Application configuration with YAML and environment variable support.\"\"\"\n\n    github_token: str | None = Field(\n        default=None,\n        validation_alias=AliasChoices(\n            \"GITDEVFLOW_GITHUB_TOKEN\", \"GITHUB_TOKEN\", \"github_token\"\n        ),\n    )\n\n    @field_validator(\"github_token\", mode=\"before\")\n    @classmethod\n    def sanitize_token(cls, v: str | None) -&gt; str | None:\n        if isinstance(v, str):\n            cleaned = \"\".join(c for c in v if 32 &lt;= ord(c) &lt;= 126).strip()\n            return cleaned if cleaned else None\n        return v\n\n    default_repo: str | None = Field(\n        default=None,\n        validation_alias=AliasChoices(\"DEFAULT_REPO\", \"default_repo\"),\n    )\n    pr_label_prefix: str = Field(\n        default=\"type:\",\n        validation_alias=AliasChoices(\"PR_LABEL_PREFIX\", \"pr_label_prefix\"),\n    )\n    changelog_sections: dict[str, list[str]] = Field(\n        default_factory=lambda: {\n            \"Features\": [\"feat\", \"enhancement\"],\n            \"Bug Fixes\": [\"fix\", \"bug\"],\n            \"Documentation\": [\"docs\", \"documentation\"],\n        }\n    )\n    output_format: Literal[\"rich\", \"plain\"] = Field(\n        default=\"rich\",\n        validation_alias=AliasChoices(\"OUTPUT_FORMAT\", \"output_format\"),\n    )\n\n    model_config = SettingsConfigDict(\n        env_file=\".env\",\n        env_file_encoding=\"utf-8\",\n        extra=\"ignore\",\n    )\n\n    def save(self, path: str | Path | None = None) -&gt; Path:\n        \"\"\"Save configuration to YAML file with strict file permissions (0o600).\n\n        Args:\n            path: Optional destination path.\n\n        Returns:\n            The Path object where configuration was written.\n        \"\"\"\n        target_path = Path(path) if path else DEFAULT_CONFIG_PATH\n        target_path.parent.mkdir(parents=True, exist_ok=True)\n\n        data = self.model_dump()\n        yaml_content = yaml.safe_dump(data, sort_keys=False)\n        target_path.write_text(yaml_content, encoding=\"utf-8\")\n\n        with suppress(OSError, NotImplementedError):\n            os.chmod(target_path, 0o600)\n\n        return target_path\n\n    @classmethod\n    def load(cls, path: str | Path | None = None) -&gt; AppConfig:\n        \"\"\"Load configuration from a YAML file overlaid with environment variables.\n\n        Args:\n            path: Optional custom path to YAML config file.\n\n        Returns:\n            Populated AppConfig instance.\n        \"\"\"\n        config_path = Path(path) if path else DEFAULT_CONFIG_PATH\n        if not config_path.is_file() and not path and LEGACY_CONFIG_PATH.is_file():\n            config_path = LEGACY_CONFIG_PATH\n\n        yaml_data: dict[str, Any] = {}\n\n        if config_path.is_file():\n            # Check file permissions on POSIX systems\n            try:\n                if os.name == \"posix\":\n                    st = config_path.stat()\n                    if st.st_mode &amp; (stat.S_IRWXG | stat.S_IRWXO) != 0:\n                        from gitdevflow.utils.console import error_console\n\n                        error_console.print(\n                            f\"[bold yellow]Warning:[/] Config file '{config_path}' \"\n                            \"permissions are too open. Run 'chmod 600 \"\n                            f\"{config_path}' to secure it.\"\n                        )\n            except Exception:\n                pass\n\n            try:\n                with open(config_path, encoding=\"utf-8\") as f:\n                    content = yaml.safe_load(f)\n                    if isinstance(content, dict):\n                        yaml_data = content\n            except Exception as err:\n                from gitdevflow.core.exceptions import ConfigError\n\n                raise ConfigError(\n                    f\"Failed to parse config file {config_path}: {err}\"\n                ) from err\n\n        # Environment variables override YAML values (12-factor app rule)\n        for key in list(yaml_data.keys()):\n            upper_key = key.upper()\n            if upper_key in os.environ:\n                yaml_data[key] = os.environ[upper_key]\n            elif f\"GITDEVFLOW_{upper_key}\" in os.environ:\n                yaml_data[key] = os.environ[f\"GITDEVFLOW_{upper_key}\"]\n            elif key in os.environ:\n                yaml_data[key] = os.environ[key]\n\n        return cls(**yaml_data)\n\n    def masked_token(self) -&gt; str:\n        \"\"\"Return masked version of the GitHub token for safe display.\"\"\"\n        if not self.github_token:\n            return \"[Not Set]\"\n        token = self.github_token.strip()\n        if len(token) &lt;= 8:\n            return \"****\"\n        return f\"{token[:4]}...{token[-4:]}\"\n\n    def to_dict(self, mask_sensitive: bool = True) -&gt; dict[str, Any]:\n        \"\"\"Convert config to dictionary format with optional token masking.\"\"\"\n        data = self.model_dump()\n        if mask_sensitive:\n            data[\"github_token\"] = self.masked_token()\n        return data\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.config.AppConfig.load","title":"<code>load(path=None)</code>  <code>classmethod</code>","text":"<p>Load configuration from a YAML file overlaid with environment variables.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path | None</code> <p>Optional custom path to YAML config file.</p> <code>None</code> <p>Returns:</p> Type Description <code>AppConfig</code> <p>Populated AppConfig instance.</p> Source code in <code>src/gitdevflow/core/config.py</code> <pre><code>@classmethod\ndef load(cls, path: str | Path | None = None) -&gt; AppConfig:\n    \"\"\"Load configuration from a YAML file overlaid with environment variables.\n\n    Args:\n        path: Optional custom path to YAML config file.\n\n    Returns:\n        Populated AppConfig instance.\n    \"\"\"\n    config_path = Path(path) if path else DEFAULT_CONFIG_PATH\n    if not config_path.is_file() and not path and LEGACY_CONFIG_PATH.is_file():\n        config_path = LEGACY_CONFIG_PATH\n\n    yaml_data: dict[str, Any] = {}\n\n    if config_path.is_file():\n        # Check file permissions on POSIX systems\n        try:\n            if os.name == \"posix\":\n                st = config_path.stat()\n                if st.st_mode &amp; (stat.S_IRWXG | stat.S_IRWXO) != 0:\n                    from gitdevflow.utils.console import error_console\n\n                    error_console.print(\n                        f\"[bold yellow]Warning:[/] Config file '{config_path}' \"\n                        \"permissions are too open. Run 'chmod 600 \"\n                        f\"{config_path}' to secure it.\"\n                    )\n        except Exception:\n            pass\n\n        try:\n            with open(config_path, encoding=\"utf-8\") as f:\n                content = yaml.safe_load(f)\n                if isinstance(content, dict):\n                    yaml_data = content\n        except Exception as err:\n            from gitdevflow.core.exceptions import ConfigError\n\n            raise ConfigError(\n                f\"Failed to parse config file {config_path}: {err}\"\n            ) from err\n\n    # Environment variables override YAML values (12-factor app rule)\n    for key in list(yaml_data.keys()):\n        upper_key = key.upper()\n        if upper_key in os.environ:\n            yaml_data[key] = os.environ[upper_key]\n        elif f\"GITDEVFLOW_{upper_key}\" in os.environ:\n            yaml_data[key] = os.environ[f\"GITDEVFLOW_{upper_key}\"]\n        elif key in os.environ:\n            yaml_data[key] = os.environ[key]\n\n    return cls(**yaml_data)\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.config.AppConfig.masked_token","title":"<code>masked_token()</code>","text":"<p>Return masked version of the GitHub token for safe display.</p> Source code in <code>src/gitdevflow/core/config.py</code> <pre><code>def masked_token(self) -&gt; str:\n    \"\"\"Return masked version of the GitHub token for safe display.\"\"\"\n    if not self.github_token:\n        return \"[Not Set]\"\n    token = self.github_token.strip()\n    if len(token) &lt;= 8:\n        return \"****\"\n    return f\"{token[:4]}...{token[-4:]}\"\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.config.AppConfig.save","title":"<code>save(path=None)</code>","text":"<p>Save configuration to YAML file with strict file permissions (0o600).</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path | None</code> <p>Optional destination path.</p> <code>None</code> <p>Returns:</p> Type Description <code>Path</code> <p>The Path object where configuration was written.</p> Source code in <code>src/gitdevflow/core/config.py</code> <pre><code>def save(self, path: str | Path | None = None) -&gt; Path:\n    \"\"\"Save configuration to YAML file with strict file permissions (0o600).\n\n    Args:\n        path: Optional destination path.\n\n    Returns:\n        The Path object where configuration was written.\n    \"\"\"\n    target_path = Path(path) if path else DEFAULT_CONFIG_PATH\n    target_path.parent.mkdir(parents=True, exist_ok=True)\n\n    data = self.model_dump()\n    yaml_content = yaml.safe_dump(data, sort_keys=False)\n    target_path.write_text(yaml_content, encoding=\"utf-8\")\n\n    with suppress(OSError, NotImplementedError):\n        os.chmod(target_path, 0o600)\n\n    return target_path\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.config.AppConfig.to_dict","title":"<code>to_dict(mask_sensitive=True)</code>","text":"<p>Convert config to dictionary format with optional token masking.</p> Source code in <code>src/gitdevflow/core/config.py</code> <pre><code>def to_dict(self, mask_sensitive: bool = True) -&gt; dict[str, Any]:\n    \"\"\"Convert config to dictionary format with optional token masking.\"\"\"\n    data = self.model_dump()\n    if mask_sensitive:\n        data[\"github_token\"] = self.masked_token()\n    return data\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.config.load_config","title":"<code>load_config(path=None)</code>","text":"<p>Convenience wrapper around AppConfig.load.</p> Source code in <code>src/gitdevflow/core/config.py</code> <pre><code>def load_config(path: str | Path | None = None) -&gt; AppConfig:\n    \"\"\"Convenience wrapper around AppConfig.load.\"\"\"\n    return AppConfig.load(path)\n</code></pre>"},{"location":"reference/api/#models","title":"Models","text":""},{"location":"reference/api/#gitdevflow.core.models","title":"<code>gitdevflow.core.models</code>","text":"<p>Pydantic models for GitHub API resources.</p>"},{"location":"reference/api/#gitdevflow.core.models.BranchComparison","title":"<code>BranchComparison</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>GitHub branch comparison model.</p> Source code in <code>src/gitdevflow/core/models.py</code> <pre><code>class BranchComparison(BaseModel):\n    \"\"\"GitHub branch comparison model.\"\"\"\n\n    ahead_by: int = 0\n    behind_by: int = 0\n    status: str = \"\"\n    total_commits: int = 0\n    commits: list[Commit] = Field(default_factory=list)\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.models.Commit","title":"<code>Commit</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>GitHub commit model.</p> Source code in <code>src/gitdevflow/core/models.py</code> <pre><code>class Commit(BaseModel):\n    \"\"\"GitHub commit model.\"\"\"\n\n    sha: str\n    commit: CommitDetail | None = None\n    html_url: str | None = None\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.models.CommitDetail","title":"<code>CommitDetail</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Inner commit details.</p> Source code in <code>src/gitdevflow/core/models.py</code> <pre><code>class CommitDetail(BaseModel):\n    \"\"\"Inner commit details.\"\"\"\n\n    message: str\n    author: dict[str, Any] | None = None\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.models.Label","title":"<code>Label</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>GitHub issue/PR label model.</p> Source code in <code>src/gitdevflow/core/models.py</code> <pre><code>class Label(BaseModel):\n    \"\"\"GitHub issue/PR label model.\"\"\"\n\n    name: str\n    color: str = \"\"\n    description: str | None = None\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.models.PullRequest","title":"<code>PullRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>GitHub pull request model.</p> Source code in <code>src/gitdevflow/core/models.py</code> <pre><code>class PullRequest(BaseModel):\n    \"\"\"GitHub pull request model.\"\"\"\n\n    number: int\n    title: str\n    state: str\n    html_url: str\n    user: User\n    body: str | None = None\n    labels: list[Label] = Field(default_factory=list)\n    head: PullRequestRef | None = None\n    base: PullRequestRef | None = None\n    draft: bool = False\n    created_at: str | None = None\n    merged_at: str | None = None\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.models.PullRequestRef","title":"<code>PullRequestRef</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Head or base reference in a pull request.</p> Source code in <code>src/gitdevflow/core/models.py</code> <pre><code>class PullRequestRef(BaseModel):\n    \"\"\"Head or base reference in a pull request.\"\"\"\n\n    ref: str\n    sha: str\n    label: str | None = None\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.models.Repository","title":"<code>Repository</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>GitHub repository model.</p> Source code in <code>src/gitdevflow/core/models.py</code> <pre><code>class Repository(BaseModel):\n    \"\"\"GitHub repository model.\"\"\"\n\n    id: int\n    name: str\n    full_name: str\n    owner: User\n    html_url: str\n    default_branch: str = \"main\"\n    description: str | None = None\n    private: bool = False\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.models.User","title":"<code>User</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>GitHub user model.</p> Source code in <code>src/gitdevflow/core/models.py</code> <pre><code>class User(BaseModel):\n    \"\"\"GitHub user model.\"\"\"\n\n    login: str\n    id: int\n    avatar_url: str | None = None\n    html_url: str | None = None\n</code></pre>"},{"location":"reference/api/#exceptions","title":"Exceptions","text":""},{"location":"reference/api/#gitdevflow.core.exceptions","title":"<code>gitdevflow.core.exceptions</code>","text":"<p>Custom exception hierarchy for gitdevflow.</p>"},{"location":"reference/api/#gitdevflow.core.exceptions.AuthenticationError","title":"<code>AuthenticationError</code>","text":"<p>               Bases: <code>GitHubAPIError</code></p> <p>Raised when GitHub API authentication fails (HTTP 401).</p> Source code in <code>src/gitdevflow/core/exceptions.py</code> <pre><code>class AuthenticationError(GitHubAPIError):\n    \"\"\"Raised when GitHub API authentication fails (HTTP 401).\"\"\"\n\n    def __init__(self, message: str = \"Invalid or expired token\") -&gt; None:\n        super().__init__(status_code=401, message=message)\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.exceptions.ConfigError","title":"<code>ConfigError</code>","text":"<p>               Bases: <code>GitDevFlowError</code></p> <p>Raised when configuration is invalid or missing.</p> Source code in <code>src/gitdevflow/core/exceptions.py</code> <pre><code>class ConfigError(GitDevFlowError):\n    \"\"\"Raised when configuration is invalid or missing.\"\"\"\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.exceptions.GitDevFlowError","title":"<code>GitDevFlowError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all gitdevflow errors.</p> Source code in <code>src/gitdevflow/core/exceptions.py</code> <pre><code>class GitDevFlowError(Exception):\n    \"\"\"Base exception for all gitdevflow errors.\"\"\"\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.exceptions.GitHubAPIError","title":"<code>GitHubAPIError</code>","text":"<p>               Bases: <code>GitDevFlowError</code></p> <p>Base exception for GitHub REST API errors.</p> Source code in <code>src/gitdevflow/core/exceptions.py</code> <pre><code>class GitHubAPIError(GitDevFlowError):\n    \"\"\"Base exception for GitHub REST API errors.\"\"\"\n\n    def __init__(self, status_code: int, message: str) -&gt; None:\n        self.status_code = status_code\n        self.message = message\n        super().__init__(f\"GitHub API Error [{status_code}]: {message}\")\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.exceptions.NotFoundError","title":"<code>NotFoundError</code>","text":"<p>               Bases: <code>GitHubAPIError</code></p> <p>Raised when a requested GitHub resource is not found (HTTP 404).</p> Source code in <code>src/gitdevflow/core/exceptions.py</code> <pre><code>class NotFoundError(GitHubAPIError):\n    \"\"\"Raised when a requested GitHub resource is not found (HTTP 404).\"\"\"\n\n    def __init__(self, message: str = \"Resource not found\") -&gt; None:\n        super().__init__(status_code=404, message=message)\n</code></pre>"},{"location":"reference/api/#gitdevflow.core.exceptions.RateLimitedError","title":"<code>RateLimitedError</code>","text":"<p>               Bases: <code>GitHubAPIError</code></p> <p>Raised when GitHub API rate limit is exceeded (HTTP 429 / 403).</p> Source code in <code>src/gitdevflow/core/exceptions.py</code> <pre><code>class RateLimitedError(GitHubAPIError):\n    \"\"\"Raised when GitHub API rate limit is exceeded (HTTP 429 / 403).\"\"\"\n\n    def __init__(\n        self,\n        message: str = \"GitHub API rate limit exceeded\",\n        retry_after: int | None = None,\n    ) -&gt; None:\n        self.retry_after = retry_after\n        super().__init__(status_code=429, message=message)\n</code></pre>"},{"location":"reference/api/#utilities","title":"Utilities","text":""},{"location":"reference/api/#gitdevflow.utils.formatting","title":"<code>gitdevflow.utils.formatting</code>","text":"<p>Date, markdown, and string formatting helpers.</p>"},{"location":"reference/api/#gitdevflow.utils.formatting.format_date","title":"<code>format_date(dt, fmt='%Y-%m-%d')</code>","text":"<p>Format a datetime object to a string.</p> <p>Parameters:</p> Name Type Description Default <code>dt</code> <code>datetime</code> <p>The datetime to format.</p> required <code>fmt</code> <code>str</code> <p>The strftime format string.</p> <code>'%Y-%m-%d'</code> <p>Returns:</p> Type Description <code>str</code> <p>Formatted date string.</p> Source code in <code>src/gitdevflow/utils/formatting.py</code> <pre><code>def format_date(dt: datetime, fmt: str = \"%Y-%m-%d\") -&gt; str:\n    \"\"\"Format a datetime object to a string.\n\n    Args:\n        dt: The datetime to format.\n        fmt: The strftime format string.\n\n    Returns:\n        Formatted date string.\n    \"\"\"\n    return dt.strftime(fmt)\n</code></pre>"},{"location":"reference/api/#gitdevflow.utils.formatting.slugify","title":"<code>slugify(text)</code>","text":"<p>Convert a string to a URL-friendly slug.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>The input string.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Lowercased, hyphen-separated slug.</p> Source code in <code>src/gitdevflow/utils/formatting.py</code> <pre><code>def slugify(text: str) -&gt; str:\n    \"\"\"Convert a string to a URL-friendly slug.\n\n    Args:\n        text: The input string.\n\n    Returns:\n        Lowercased, hyphen-separated slug.\n    \"\"\"\n    import re\n\n    text = text.lower().strip()\n    text = re.sub(r\"[^\\w\\s-]\", \"\", text)\n    return re.sub(r\"[-\\s]+\", \"-\", text)\n</code></pre>"},{"location":"reference/api/#gitdevflow.utils.formatting.truncate","title":"<code>truncate(text, max_length=80, suffix='...')</code>","text":"<p>Truncate a string to a maximum length.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>The input string.</p> required <code>max_length</code> <code>int</code> <p>Maximum allowed length.</p> <code>80</code> <code>suffix</code> <code>str</code> <p>Suffix to append when truncated.</p> <code>'...'</code> <p>Returns:</p> Type Description <code>str</code> <p>Truncated string.</p> Source code in <code>src/gitdevflow/utils/formatting.py</code> <pre><code>def truncate(text: str, max_length: int = 80, suffix: str = \"...\") -&gt; str:\n    \"\"\"Truncate a string to a maximum length.\n\n    Args:\n        text: The input string.\n        max_length: Maximum allowed length.\n        suffix: Suffix to append when truncated.\n\n    Returns:\n        Truncated string.\n    \"\"\"\n    if len(text) &lt;= max_length:\n        return text\n    return text[: max_length - len(suffix)] + suffix\n</code></pre>"},{"location":"reference/api/#gitdevflow.utils.formatting.utc_now","title":"<code>utc_now()</code>","text":"<p>Return the current UTC datetime.</p> Source code in <code>src/gitdevflow/utils/formatting.py</code> <pre><code>def utc_now() -&gt; datetime:\n    \"\"\"Return the current UTC datetime.\"\"\"\n    return datetime.now(tz=timezone.utc)\n</code></pre>"},{"location":"usage/changelog/","title":"Changelog","text":""},{"location":"usage/changelog/#overview","title":"Overview","text":"<p>Automatically generate changelogs from merged pull requests.</p>"},{"location":"usage/changelog/#commands","title":"Commands","text":""},{"location":"usage/changelog/#gitdevflow-changelog-generate","title":"<code>gitdevflow changelog generate</code>","text":"<p>Generate a changelog from merged PRs since the last release.</p> <pre><code>gitdevflow changelog generate --since v0.1.0\n</code></pre> <p>Options:</p> Flag Description Default <code>--since</code> Start tag/commit Last tag <code>--output</code> Output file <code>CHANGELOG.md</code> <code>--format</code> Output format (<code>md</code>, <code>json</code>) <code>md</code> <code>--group-by</code> Group entries by label <code>true</code>"},{"location":"usage/changelog/#gitdevflow-changelog-validate","title":"<code>gitdevflow changelog validate</code>","text":"<p>Validate the existing changelog format.</p> <pre><code>gitdevflow changelog validate\n</code></pre>"},{"location":"usage/config/","title":"Configuration","text":""},{"location":"usage/config/#overview","title":"Overview","text":"<p>gitdevflow uses a YAML configuration file with environment variable overrides.</p>"},{"location":"usage/config/#configuration-file","title":"Configuration File","text":"<p>By default, gitdevflow looks for <code>.gitdevflow.yml</code> in your project root.</p> <pre><code># .gitdevflow.yml\ngithub:\n  owner: BlockForge-Dev\n  repo: your-repo\n  token: ${GITHUB_TOKEN}  # Environment variable reference\n\npr:\n  default_base: main\n  template: .github/PULL_REQUEST_TEMPLATE.md\n  labels:\n    - auto-merge\n\nchangelog:\n  output: CHANGELOG.md\n  group_by_labels: true\n  categories:\n    - name: \"\u2728 Features\"\n      labels: [\"enhancement\", \"feature\"]\n    - name: \"\ud83d\udc1b Bug Fixes\"\n      labels: [\"bug\", \"fix\"]\n    - name: \"\ud83d\udcda Documentation\"\n      labels: [\"docs\", \"documentation\"]\n</code></pre>"},{"location":"usage/config/#environment-variables","title":"Environment Variables","text":"Variable Description <code>GITHUB_TOKEN</code> GitHub personal access token <code>GITDEVFLOW_CONFIG</code> Custom config file path"},{"location":"usage/config/#commands","title":"Commands","text":""},{"location":"usage/config/#gitdevflow-config-show","title":"<code>gitdevflow config show</code>","text":"<p>Display the current configuration.</p> <pre><code>gitdevflow config show\n</code></pre>"},{"location":"usage/config/#gitdevflow-config-validate","title":"<code>gitdevflow config validate</code>","text":"<p>Validate the configuration file.</p> <pre><code>gitdevflow config validate\n</code></pre>"},{"location":"usage/pr/","title":"PR Commands","text":""},{"location":"usage/pr/#overview","title":"Overview","text":"<p>Manage pull requests directly from your terminal.</p>"},{"location":"usage/pr/#commands","title":"Commands","text":""},{"location":"usage/pr/#gitdevflow-pr-create","title":"<code>gitdevflow pr create</code>","text":"<p>Create a new pull request.</p> <pre><code>gitdevflow pr create --title \"Add feature X\" --base main\n</code></pre> <p>Options:</p> Flag Description Default <code>--title</code> PR title Required <code>--base</code> Base branch <code>main</code> <code>--draft</code> Create as draft <code>false</code> <code>--body</code> PR description Empty"},{"location":"usage/pr/#gitdevflow-pr-list","title":"<code>gitdevflow pr list</code>","text":"<p>List open pull requests.</p> <pre><code>gitdevflow pr list --state open --limit 10\n</code></pre>"},{"location":"usage/pr/#gitdevflow-pr-merge","title":"<code>gitdevflow pr merge</code>","text":"<p>Merge a pull request.</p> <pre><code>gitdevflow pr merge 42 --strategy squash\n</code></pre>"}]}